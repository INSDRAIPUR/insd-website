<div id="client-logos" class="feat-logos-section">
    <div class="feat-logos-container">
      <h2 class="client-logos-heading fontxl">Featured In</h2>
      <div class="client-logos-marquee-wrapper">
        <!-- Marquee track (duplicated for seamless looping) -->
        <div class="client-logos-track">
          <% feat.forEach(item => { %>
            <a href="<%= item.hyperlink || '' %>" class="client-logo-item">
              <img src="<%= process.env.S3_BASE_URL + item.logo %>" alt="<%= item.title %>" class="feat-logo-img">
            </a>
          <% }) %>
          <!-- Duplicate the logos for seamless looping -->
          <% feat.forEach(item => { %>
            <a href="<%= item.hyperlink || '' %>" class="client-logo-item">
              <img src="<%= process.env.S3_BASE_URL + item.logo %>" alt="<%= item.title %>" class="feat-logo-img">
            </a>
          <% }) %>
        </div>
      </div>
    </div>
  </div>
  
  <style>
    h2{
      font-family: "Degular Display",sans-serif;
      font-weight: 600;
    }

    .fontxl{
        font-size: 32px !important;
    }
    /* Base styles */
    .feat-logos-section {
      padding: 0rem 0 2rem 0;
      overflow: hidden;
    }
    
    .feat-logos-container {
      max-width: 1200px;
      margin: 40px auto 60px auto;
     /*  padding: 0 1rem;*/
    }
    
    .client-logos-heading {
      text-align: center;
      margin-bottom: 2rem;
      font-size: 1.25rem;
    }
    
    /* Marquee styles */
    .client-logos-marquee-wrapper {
      position: relative;
      overflow: hidden;
    }
    
    .client-logos-track {
      display: flex;
      margin: 40px 0;
      align-items: center;
      width: max-content;
      animation: scroll-logos 30s linear infinite;
      will-change: transform;
    }
    
    .client-logo-item {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 2rem;
      flex-shrink: 0;
    }
    
    .feat-logo-img {
      height: 100px;
      width: auto;
    
      object-fit: contain;
    }
    
    /* Animation */
    @keyframes scroll-logos {
      0% { transform: translateX(0); }
      100% { transform: translateX(-50%); }
    }
    
    /* Responsive adjustments */
    @media (max-width: 768px) {
      .client-logo-item {
        margin: 0 1rem;
      }
      .feat-logo-img {
        height: 70px;
      }
      @keyframes scroll-logos {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
      }
    }
  </style>